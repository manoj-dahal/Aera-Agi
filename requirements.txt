fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
pydantic-settings>=2.2
PyYAML>=6.0
httpx>=0.27
cryptography>=42.0
python-multipart>=0.0.9

# Optional: richer host metrics for the PC Information panel.
# Telemetry falls back to /proc when this is absent.
psutil>=5.9
