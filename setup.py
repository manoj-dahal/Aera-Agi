"""Setup shim for AERA AGI.

All project metadata lives in pyproject.toml; this file exists only for
tooling that still expects a setup.py (e.g. `pip install -e .` on older pips).
"""

from setuptools import setup

setup()
